    // let convData = {
    //     "ownerId": ownerId,
    //     "participants": arr, 
    // }

    // db.users.find({ awards: { $elemMatch: {award:'Turing Award', year:1977} } })

    // Conversation.update({ participants: { $elemMatch: arr }, {...req.body, token}, {upsert: true},(err, result) => {
    //     if (err) {
    //               res.json({ isAuth: false, error: true });
    //       } else {
    //         req.user = result;
    //         req.token = token;
    //         next();
    //       }
    // })

    // Conversation.create({ownerId, participants: arr, timestamp: new Date().toISOString()}, (err, data) => {
    //     if(err){
    //         res.status(500).send(err)
    //     } else {
    //         res.status(201).send(data);
    //     }
    // })


    